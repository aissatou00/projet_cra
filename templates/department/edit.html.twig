{# templates/department/edit.html.twig #}

{% extends 'base.html.twig' %}

{% block title %}Modifier le Département{% endblock %}

{% block body %}
    <h1>Modifier le Département</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-primary">Enregistrer</button>
    {{ form_end(form) }}
    <a href="{{ path('department_index') }}">Retour</a>
{% endblock %}
